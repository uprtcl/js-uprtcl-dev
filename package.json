{
  "name": "root",
  "private": true,
  "scripts": {
    "dev": "lerna run dev --parallel",
    "update-uprtcl": "lerna run update-uprtcl --parallel"
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "npm-check-updates": "^10.2.1"
  }
}
